根据以下代码变更（git diff / 新增文件内容），完成两件事：

1. 错误摘要：用一句话列出是否存在语法错误或明显逻辑错误；若没有则只写「无」。
2. 提交描述：生成一行简洁的 git commit message，可直接用于 git commit -m "..."。

请严格按以下格式输出，不要任何其他内容、解释或换行：
【错误摘要】：<每有一个就列出一条，如那个文件第几行有什么问题，每一点都要换行，没有则写「无」>
<换行>
【提交描述】：<每有一个就列出一条 commit message, 如1、添加xx选项， 2、接入xx动画效果，每一点都要换行，没有则写「无」>

变更内容：
\`\`\`
{{DIFF}}
\`\`\``;